#!/bin/bash
#
#

echo "Removing stray react-native modules"
sudo find app_js -name react-native -exec rm -rf {} \;
echo "Restarting packagers"
for i in `sudo docker ps | grep packager | awk '{print $12}' | xargs`; do sudo docker restart $i; done
